<div class="well-lg">



<div class="col-md-12">
  <b><i class="fa fa-clock-o" aria-hidden="true"></i>
    Time:</b></br>
  <p><%= appointment.date %></p>
</div>















<hr>

<% if user_signed_in? %>
  <% if appointment.user_id == current_user.id %>

  <%= link_to "Edit", edit_patient_appointment_path(appointment.patient, appointment) %>

  <%= link_to "Delete", patient_appointment_path(appointment.patient, appointment), method: :delete, data: { confirm: "Are you sure?"} %>

  <% end %>


<% end %>


</div>
